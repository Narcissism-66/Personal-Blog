spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog
    #jdbc????? ????jdbc:mysql://????????:??/????
    username: root
    #?????
    password: 19805601056Bkj@@
    #?????
    driver-class-name: com.mysql.cj.jdbc.Driver
    #??????

#???MySQL????????????????????
  web:
    resources:
      static-locations:
        - classpath:/static/
        - file:${file.upload-dir} # 将上传目录配置为静态资源路径

file:
  upload-dir: ./uploads # 上传目录路径

# 代码执行相关配置
code:
  execution:
    timeout: 5000 # 代码执行超时时间，单位毫秒
    tmp:
      dir: ./code # 存放临时代码文件的目录
    memory-limit: 128M # 内存限制
    clean-interval: 3600000 # 定时清理临时文件的间隔，单位毫秒