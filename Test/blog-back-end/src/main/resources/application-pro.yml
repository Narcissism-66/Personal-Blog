spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog
    #jdbc????? ????jdbc:mysql://????????:??/????
    username: blog
    #?????
    password: 19805601056Bkj@@
    #?????
    driver-class-name: com.mysql.cj.jdbc.Driver
    #??????

#???MySQL????????????????????
  web:
    resources:
      static-locations:
        - classpath:/static/
        - file:${file.upload-dir} # 将上传目录配置为静态资源路径

file:
  upload-dir: ./uploads # 上传目录路径

# 代码执行配置
code:
  execution:
    timeout: 10000 # 代码执行超时时间（毫秒）
    tmp:
      dir: ./code # 临时文件目录
    clean-interval: 3600000 # 清理间隔（毫秒）